# Family Archive stack environment variables for Portainer
# Copy these into Portainer stack environment variables.
# Do not commit real secrets to Git.

TZ=America/Los_Angeles

# NFS server and remote family archive root
NFS_SERVER=192.168.86.82
NFS_EXPORT_ROOT=/data/apps/family-archive

# Immich
IMMICH_HTTP_PORT=2283
IMMICH_VERSION=v3
IMMICH_DB_STORAGE_TYPE=SSD
IMMICH_DB_USERNAME=postgres
IMMICH_DB_NAME=immich
# Generate with: openssl rand -hex 32
# Use only A-Z, a-z, and 0-9 for maximum compatibility.
IMMICH_DB_PASSWORD=change-me

# Gramps Web
GRAMPSWEB_HTTP_PORT=5000
GRAMPSWEB_VERSION=latest
GRAMPSWEB_CELERY_CONCURRENCY=2

# Gramps PostgreSQL
GRAMPS_POSTGRES_VERSION=latest
# Generate three different values with: openssl rand -hex 32
GRAMPS_POSTGRES_ADMIN_PASSWORD=change-me
GRAMPS_POSTGRES_PASSWORD=change-me
GRAMPS_POSTGRES_USER_PASSWORD=change-me
